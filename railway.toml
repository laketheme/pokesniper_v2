[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
cd /app && uvicorn main:app --host 0.0.0.0 --port 8080 --workers 1
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[healthcheck]
path = "/health"
timeout = 10
